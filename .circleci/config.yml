version: 2
workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
jobs:
  one:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "a> A first hello"
  two:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "b> A first hello"
